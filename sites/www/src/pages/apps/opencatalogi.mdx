---
title: OpenCatalogi, Conduction
description: Federated data catalogue for apps, datasets, APIs. Public-facing search at your Nextcloud, federation to data.overheid.nl out of the box.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, FeatureGrid, PairRow, PairCard, Pipeline, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase} from '@conduction/docusaurus-preset/components';

<DetailHero
  appId="opencatalogi"
  crumb={[{label: 'Apps', href: '/apps'}, 'OpenCatalogi']}
  status={{label: 'Stable', color: 'var(--c-mint-500)'}}
  version="v2.4"
  locales="NL · EN"
  title="OpenCatalogi"
  tagline={<>The public-facing software, dataset, and API catalogue. Drops into your <span className="next-blue">Nextcloud</span>, surfaces every register as a searchable entry, federates to data.overheid.nl out of the box.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://docs.conduction.nl/opencatalogi'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://github.com/ConductionNL/opencatalogi'}}
  iconColor="var(--c-blue-cobalt)"
  icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4M3 17l9 4 9-4"/></svg>}
  illustration={<AppMock app="opencatalogi" />}
/>

<Section spacing="default">
  <SectionHead
    eyebrow="What it does"
    title="One catalogue, every register, every public link."
    align="stack"
    lede="Connect OpenCatalogi to your registers, configure once, and your team is publishing searchable entries the same morning. Citizens land at jouwgemeente.nl/catalogi and find what they need without writing a Woo-verzoek."
  />
  <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"/><path d="M21 21l-5-5"/></svg>}
      title="Federated search by default."
    >
      Search runs across every register on your install plus every catalogue you've federated with. Results stay sovereign, no central index.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4M3 17l9 4 9-4"/></svg>}
      title="DCAT-AP profile, NLDS-aligned."
    >
      Every entry exports as DCAT-AP and NLDS metadata. data.overheid.nl picks it up without manual upload.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M12 2l3 6 6 1-4.5 4.5 1 6-5.5-3-5.5 3 1-6L3 9l6-1z"/></svg>}
      title="Citation-stable URLs."
    >
      Every catalogue entry gets a permanent identifier. Tweede Kamer-vragen and audit trails keep working through reorganisations.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3a14 14 0 0 1 0 18M12 3a14 14 0 0 0 0 18"/></svg>}
      title="WOO publication flow."
    >
      Eleven Woo-categorieën each get a register; the catalogue surfaces them at jouwgemeente.nl/woo with audit log and retention policies built in.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M21 11.5a8.4 8.4 0 0 1-9 8.4 8.4 8.4 0 0 1-3.4-.7L3 21l1.5-5.4A8.4 8.4 0 1 1 21 11.5z"/></svg>}
      title="Open, forever."
    >
      Every entry exportable to plain JSON. The day you uninstall is as easy as the day you installed. No vendor lock-in, ever.
    </FeatureItem>
  </FeatureList>
</Section>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="How it works"
    title="Four steps, one configuration pass."
    align="stack"
    lede="The same pipeline applies to a software catalogue, a Woo-portal, or a public API directory. Configure the connector, point it at your register, the catalogue lights up."
  />
  <Pipeline
    start={{label: 'Sources', items: ['Spreadsheets', 'Databases', 'REST / SOAP', 'Legacy systems']}}
    steps={[
      {number: '01', kicker: 'Ingest', name: 'OpenConnector', caption: 'Pull data in from existing systems via REST, SOAP, file drops.'},
      {number: '02', kicker: 'Validate', name: 'OpenRegister', caption: 'Schema validates every write. Citation-stable IDs assigned.'},
      {number: '03', kicker: 'Surface', name: 'OpenCatalogi', caption: 'Federated public catalogue. Search, filter, share.'},
      {number: '04', kicker: 'Render', name: 'MyDash', caption: 'Optional dashboards and reports for the team.'},
    ]}
    end={{label: 'Consumers', items: ['Citizens', 'Partners', 'API clients', 'Auditors']}}
  />
</Section>

<RotatingCards
  eyebrow="Three ways OpenCatalogi earns its place"
  title="Federated catalogue, register-aware, dashboard-ready."
  lede="Scroll the cards. Each pins as you reach it; the next slides up to cover."
  cards={[
    {
      tone: 'forest',
      eyebrow: 'Federated by default',
      title: 'One catalogue, many publishers.',
      summary: 'OpenCatalogi indexes registers across organisations and federates to data.overheid.nl. Search once, find every published record. No central database, no nightly import.',
      cta: {label: 'See the catalogue', href: '/apps/opencatalogi'},
      panel: <AppMock app="opencatalogi" />,
    },
    {
      tone: 'lavender',
      eyebrow: 'Register-aware',
      title: 'Every entry is a typed register row.',
      summary: 'OpenCatalogi reads OpenRegister directly. Schema changes flow through. Citation-stable IDs land in the catalogue automatically.',
      cta: {label: 'See OpenRegister', href: '/apps/openregister'},
      panel: <AppMock app="openregister" />,
    },
    {
      tone: 'workspace',
      eyebrow: 'Dashboard-ready',
      title: 'What you publish, you can chart.',
      summary: 'MyDash reads the same registers OpenCatalogi indexes. Publish a dataset and a graph of it shows up on the dashboard the same morning.',
      cta: {label: 'See MyDash', href: '/apps/mydash'},
      panel: <AppMock app="mydash-bi" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="Widgets we ship"
  title="On every Nextcloud dashboard."
  lede="Install OpenCatalogi and the home screen tracks what got published, what was searched, and what other gemeenten are federating from you."
  widgets={[
    {
      title: 'Recent publications',
      desc: 'Last items added to the public catalogue. Category hex, title, federation status.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {['var(--c-blue-cobalt)', 'var(--c-forest-500)', 'var(--c-lavender-500)', 'var(--c-terracotta-500)', 'var(--c-mint-500)'].map((tone, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 4 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 14, height: 16, clipPath: 'var(--hex-pointy-top)', background: tone, flexShrink: 0}}></span>
              <div style={{flex: 1, height: 4, background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              <span style={{width: 8, height: 9, clipPath: 'var(--hex-pointy-top)', background: i < 3 ? 'var(--c-mint-500)' : 'var(--c-cobalt-200)'}}></span>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Catalogue stats',
      desc: 'Three KPIs at a glance: total publications, federated downstream, queries this week.',
      panel: (
        <div style={{display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 6}}>
          {[
            {n: '3.4k', l: 'records', tone: 'var(--c-cobalt-900)'},
            {n: '12',  l: 'feeders', tone: 'var(--c-mint-500)'},
            {n: '847', l: 'queries', tone: 'var(--c-orange-knvb)'},
          ].map((kpi, i) => (
            <div key={i} style={{padding: '8px 6px', background: 'var(--c-cobalt-50)', borderRadius: 'var(--radius-sm)', display: 'flex', flexDirection: 'column', gap: 2}}>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 18, fontWeight: 700, color: kpi.tone}}>{kpi.n}</div>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 9, letterSpacing: '0.06em', textTransform: 'uppercase', color: 'var(--c-cobalt-400)'}}>{kpi.l}</div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Federation status',
      desc: 'Who is consuming your catalogue, who is publishing back. Mint = healthy, orange = retry, red = down.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {tone: 'var(--c-mint-500)',       label: 'data.overheid.nl'},
            {tone: 'var(--c-mint-500)',       label: 'gemeente Tilburg'},
            {tone: 'var(--c-orange-knvb)',    label: 'gemeente Almere'},
            {tone: 'var(--c-mint-500)',       label: 'Forum Standaardisatie'},
            {tone: 'var(--c-red-vermillion)', label: 'gemeente Hoorn'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8}}>
              <span style={{width: 8, height: 9, clipPath: 'var(--hex-pointy-top)', background: row.tone, flexShrink: 0}}></span>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 11, color: 'var(--c-cobalt-700)', flex: 1}}>{row.label}</div>
              <div style={{height: 3, width: 26, background: 'var(--c-cobalt-100)', borderRadius: 1}}></div>
            </div>
          ))}
        </div>
      ),
    },
  ]}
/>

<Showcase
  eyebrow="How OpenCatalogi plugs into the workspace"
  title="One catalogue, surfaced wherever you ask."
  lede="Three integration paths that take the published catalogue beyond its own page, into the wiki, the workflow tools, and the AI assistant."
  layout="side"
  defaultOpen="xwiki"
  items={[
    {
      id: 'xwiki',
      title: 'XWiki',
      summary: 'Embed a live catalogue query inside any wiki page. Procedure docs cite the canonical dataset, not a snapshot. The wiki stays explanatory; the catalogue stays authoritative.',
      cta: {label: 'See the wiki macro', href: 'https://docs.conduction.nl/opencatalogi/xwiki'},
      panel: <AppMock app="opencatalogi" />,
    },
    {
      id: 'mail-files',
      title: 'Mail and Files',
      summary: 'Find a catalogued dataset without leaving Mail or Files. The sidebar lets you search the federated catalogue, attach a citation-stable URL to the email, drop the dataset reference into a folder.',
      cta: {label: 'See the sidebar guide', href: 'https://docs.conduction.nl/opencatalogi/mail-files'},
      panel: <AppMock app="opencatalogi" />,
    },
    {
      id: 'llms',
      title: 'LLMs (Claude, Mistral, Ollama)',
      summary: 'Ask the catalogue in plain language. The LLM resolves your question to a federated query, returns datasets with citation-stable IDs, and explains why each one matched. Helpful when the catalogue gets to thousands of items.',
      cta: {label: 'See the semantic search', href: 'https://docs.conduction.nl/opencatalogi/llm-search'},
      panel: <AppMock app="mydash-bi" />,
    },
  ]}
/>

<Section spacing="default">
  <SectionHead
    eyebrow="Pairs well with"
    title="Three apps that fill the catalogue."
    align="stack"
    lede="OpenCatalogi surfaces what's already typed. These apps fill, connect, and extend the registers OpenCatalogi reads from."
  />
  <PairRow>
    <PairCard
      href="/apps/openregister"
      icon={<svg viewBox="0 0 24 24"><ellipse cx="12" cy="6" rx="8" ry="3"/><path d="M4 6v12c0 1.7 3.6 3 8 3s8-1.3 8-3V6"/><path d="M4 12c0 1.7 3.6 3 8 3s8-1.3 8-3"/></svg>}
      name="OpenRegister"
      why="The typed-data foundation. Every catalogue entry is a register record."
    />
    <PairCard
      href="/apps/openconnector"
      icon={<svg viewBox="0 0 24 24"><circle cx="6" cy="12" r="3"/><circle cx="18" cy="6" r="3"/><circle cx="18" cy="18" r="3"/><path d="M9 12h9M9 12l9-6M9 12l9 6"/></svg>}
      name="OpenConnector"
      why="Pulls data from existing systems into registers, no glue code."
    />
    <PairCard
      href="/apps/docudesk"
      icon={<svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>}
      name="DocuDesk"
      why="Anonymises documents before they're published in the catalogue."
    />
  </PairRow>
</Section>

<CtaBanner
  title="One catalogue. Every public link."
  lede={<>Install OpenCatalogi from the <span className="next-blue">Nextcloud</span> app store, point it at your registers, and citizens find what they need the same morning.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://docs.conduction.nl/opencatalogi'}}
/>
