---
title: OpenRegister, ConNext
description: Schema-driven object store with audit trail. The data foundation underneath every workspace app.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, FeatureGrid, PairRow, PairCard, CtaBanner} from '@conduction/docusaurus-preset/components';

<DetailHero
  appId="openregister"
  crumb={[{label: 'Apps', href: '/apps'}, 'OpenRegister']}
  status={{label: 'Stable', color: 'var(--c-mint-500)'}}
  version="v3.1"
  locales="NL · EN"
  title="OpenRegister"
  tagline={<>Schemas, registers, structured data objects. The typed-data backbone underneath every other ConNext app, and the install you reach for first.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://docs.conduction.nl/openregister'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://github.com/ConductionNL/openregister'}}
  iconColor="var(--c-blue-cobalt)"
  icon={<svg viewBox="0 0 24 24"><ellipse cx="12" cy="6" rx="8" ry="3"/><path d="M4 6v12c0 1.7 3.6 3 8 3s8-1.3 8-3V6"/><path d="M4 12c0 1.7 3.6 3 8 3s8-1.3 8-3"/></svg>}
/>

<Section spacing="default">
  <SectionHead
    eyebrow="What it does"
    title="The typed-data foundation."
    align="stack"
    lede="Define a schema once. The app gives you an API, a UI, validation, an audit log, and a citation-stable identifier per record. Every other ConNext app reads and writes through that one source of truth."
  />

  <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M5 4h14v6H5zM5 14h14v6H5zM9 4v16"/></svg>}
      title="Schemas you write once."
    >
      Define a register's shape in JSON Schema. The app generates the API, validates every write, and exposes the data to other apps that speak REST.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="6" cy="12" r="3"/><circle cx="18" cy="6" r="3"/><circle cx="18" cy="18" r="3"/><path d="M9 12h9M9 12l9-6M9 12l9 6"/></svg>}
      title="One backbone, every app."
    >
      Every typed object lives in one register. Other apps consume it through the same API. No syncs to maintain, no duplicate sources of truth.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M3 12h18M12 3v18"/><circle cx="12" cy="12" r="9"/></svg>}
      title="REST and GraphQL, auto-generated."
    >
      Both APIs roll out of the schema. No controllers to write, no spec to update when the schema changes, no glue code between OpenRegister and the apps that consume it.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M12 2v4M12 18v4M2 12h4M18 12h4M5 5l3 3M16 16l3 3M5 19l3-3M16 8l3-3"/></svg>}
      title="Audit log, signed and timestamped."
    >
      Every read, write, and schema change leaves a tamper-evident trail. WOO and BIO compliance evidence ships with the install, no spreadsheet exports at audit time.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M12 2l3 6 6 1-4.5 4.5 1 6-5.5-3-5.5 3 1-6L3 9l6-1z"/></svg>}
      title="Citation-stable identifiers."
    >
      Every record gets a permanent identifier the moment you create it. Department reorganisations, system migrations, and renames don't break links to your data.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M21 11.5a8.4 8.4 0 0 1-9 8.4 8.4 8.4 0 0 1-3.4-.7L3 21l1.5-5.4A8.4 8.4 0 1 1 21 11.5z"/></svg>}
      title="Open, forever."
    >
      Issues land in public, releases are tagged, and every register you create is exportable to plain JSON. The day you uninstall is as easy as the day you installed.
    </FeatureItem>
  </FeatureList>
</Section>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Three apps that compound the install."
    align="stack"
    lede="OpenRegister is the data foundation. These apps consume it, surface it, and keep it filled."
  />
  <PairRow>
    <PairCard
      href="/apps/opencatalogi"
      icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4M3 17l9 4 9-4"/></svg>}
      name="OpenCatalogi"
      why="Surfaces every register as a public, searchable catalogue entry. Two installs, one citation strategy."
    />
    <PairCard
      href="/apps/openconnector"
      icon={<svg viewBox="0 0 24 24"><circle cx="6" cy="12" r="3"/><circle cx="18" cy="6" r="3"/><circle cx="18" cy="18" r="3"/><path d="M9 12h9M9 12l9-6M9 12l9 6"/></svg>}
      name="OpenConnector"
      why="Fills your registers from existing systems, REST, SOAP, file drops, message queues, without writing glue code."
    />
    <PairCard
      href="/apps/mydash"
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/><rect x="14" y="12" width="7" height="9"/><rect x="3" y="16" width="7" height="5"/></svg>}
      name="MyDash"
      why="Builds dashboards directly off your registers. No separate BI tool, no extra login, no ETL."
    />
  </PairRow>
</Section>

<Section spacing="default">
  <SectionHead
    eyebrow="Every feature in the box"
    title="The full spec, hover for detail."
    align="stack"
    lede="Hover (or tab to) any feature to see the full description. Status hex shows where each one is on the maturity curve."
  />
  <FeatureGrid
    legend
    items={[
      {group: 'Core capabilities'},
      {label: 'JSON Schema validation', tip: 'Define a register shape in JSON Schema. Every write is validated against it.', status: 'stable'},
      {label: 'REST API, auto-generated', tip: 'REST API rolls out of the schema. No controllers to write.', status: 'stable'},
      {label: 'GraphQL endpoint', tip: 'GraphQL endpoint also generated from the schema. Same source of truth as REST.', status: 'stable'},
      {label: 'Schema versioning', tip: 'Migrations are version-stable; old clients keep working when fields are added.', status: 'stable'},
      {label: 'Citation-stable IDs', tip: 'Every record gets a permanent identifier the moment you create it.', status: 'stable'},
      {label: 'Signed audit log', tip: 'Audit log is signed and timestamped. WOO and BIO compliance evidence ships with the install.', status: 'stable'},
      {label: 'Retention policies', tip: 'Per-record retention rules with automatic archival and deletion.', status: 'stable'},
      {label: 'JSON export / import', tip: 'Every register exportable to plain JSON. Import elsewhere with the same schema.', status: 'stable'},

      {group: 'Integrations'},
      {label: 'OpenCatalogi indexing', tip: 'OpenCatalogi indexes every register as a searchable public catalogue entry.', status: 'stable'},
      {label: 'OpenConnector ingest', tip: 'OpenConnector ingests data into registers from REST, SOAP, file drops, message queues.', status: 'stable'},
      {label: 'MyDash dashboards', tip: 'MyDash builds dashboards directly off your registers. No ETL, no separate BI tool.', status: 'stable'},
      {label: 'DocuDesk template fields', tip: 'DocuDesk reads register fields straight into document templates and signs the result.', status: 'stable'},
      {label: 'NLDS profile pack', tip: 'Twelve NLDS profile packs ship in the install: BAG, BRO, BRP, KvK, RGBZ, ZTC, etc.', status: 'beta'},
      {label: 'Federated registers', tip: 'Cross-instance register federation over signed manifests.', status: 'soon'},

      {group: 'Compliance &amp; admin'},
      {label: 'WOO evidence pack', tip: 'WOO compliance evidence (audit log + retention + citation IDs) shipped as a single export.', status: 'stable'},
      {label: 'BIO controls', tip: 'BIO control mapping covers the audit log, retention policies, role-based access.', status: 'stable'},
      {label: 'GDPR export, per data subject', tip: 'Per-subject GDPR export collects every record across registers in one ZIP.', status: 'stable'},
      {label: 'Right-to-be-forgotten', tip: 'Tombstone-style erasure with retention-policy-aware grace period.', status: 'beta'},
      {label: 'CLI migration tools', tip: 'Schema migration CLI: dry-run, diff, apply. Idempotent on every run.', status: 'stable'},
      {label: 'Scheduled imports', tip: 'Cron-style scheduled imports from external sources via OpenConnector.', status: 'soon'},
    ]}
  />
</Section>

<CtaBanner
  title="Two minutes to typed data."
  lede={<>Install OpenRegister from the <span className="next-blue">Nextcloud</span> app store, define one schema, and your team is writing structured data the same morning.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://docs.conduction.nl/openregister'}}
/>
