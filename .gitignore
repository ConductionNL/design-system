# npm workspaces
node_modules/
package-lock.json
.npm

# Docusaurus build output (sites/<name>/)
sites/*/.docusaurus
sites/*/.cache-loader
sites/*/build/

# Editor / OS
.DS_Store
*.log
.env.local
.env.*.local

# Offline-only pitch decks (confidential — never publish)
private-decks/

# Cloudflare Worker deploy artifact (rebuilt by `npm run deploy` / CI) + local state
_site/
.wrangler/
