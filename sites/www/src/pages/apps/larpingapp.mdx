---
title: LarpingApp, Conduction
description: Workflow and process orchestration for live-action role-play events. Visual designer, audit-logged, retry-safe.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase, AgentTrace, AppCrossLinks} from '@conduction/docusaurus-preset/components';

<DetailHero
  appId="larpingapp"
  crumb={[{label: 'Apps', href: '/apps'}, 'LarpingApp']}
  status={{label: 'Beta', color: 'var(--c-orange-knvb)'}}
  version="v0.1"
  locales="NL · EN"
  title="LarpingApp"
  tagline={<>Characters, rules, scenes, and NPC stats for live-action role-play (LARP). Build a setting in <span className="next-blue">Nextcloud</span>, share it with your group, and run sessions without spreadsheets-with-six-tabs.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://docs.conduction.nl/larpingapp'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://github.com/ConductionNL/larpingapp'}}
  iconColor="var(--c-blue-cobalt)"
  icon={<svg viewBox="0 0 24 24"><path d="M3 12h6l3-7 3 14 3-7h3"/></svg>}
  illustration={<AppMock app="larpingapp" />}
/>

<Section spacing="default">
  <div style={{display: 'grid', gridTemplateColumns: 'minmax(0, 1fr) 320px', gap: 'var(--space-8)', alignItems: 'start'}}>
    <div style={{minWidth: 0}}>
      <SectionHead
        eyebrow="What it does"
        title="A LARP setting that grows with your campaign."
        align="stack"
        lede="Characters, skills, items, factions, rules. All as typed registers, linked to each other, version-safe. Players edit their character in the same Nextcloud where you keep the scenes. No export-import-shuffle between tools, no separate Discord bot for stats."
      />
      <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="12" cy="8" r="4"/><path d="M4 21a8 8 0 0 1 16 0"/></svg>}
      title="Character sheets with type validation."
    >
      A character is a record against a schema. Stats, background, skills, contacts, all fields with rules. Change the schema, every character migrates with it.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M12 2l3 6 6 1-4.5 4.5 1 6-5.5-3-5.5 3 1-6L3 9l6-1z"/></svg>}
      title="Rules you write yourself."
    >
      No fixed rulebook. Define skill checks, damage formulas, and XP progression as register fields. Switching game system is a schema edit, not a rewrite.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/><rect x="14" y="12" width="7" height="9"/><rect x="3" y="16" width="7" height="5"/></svg>}
      title="Scenes, NPCs, factions, in registers."
    >
      An NPC has stats, a faction has members, a scene has a location plus participants plus outcome. All normalised, all cross-linked, all queryable.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M3 6h18M3 12h18M3 18h12"/></svg>}
      title="Audit log per scene."
    >
      Who was where, when, with what outcome. Not for compliance, for continuity. Next session you remember what actually happened.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M21 12a9 9 0 1 1-9-9"/><path d="M21 3v6h-6"/></svg>}
      title="Players work in Nextcloud."
    >
      No separate player portal. The character sheet lives in Files; chat in Talk; events in Calendar. One login for the whole campaign.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M12 2L2 7v6c0 5 3 9 10 11 7-2 10-6 10-11V7L12 2z"/></svg>}
      title="Open source, EUPL-1.2."
    >
      No yearly SaaS licence for the small organiser. Self-host on a Nextcloud you already have, or run via a Conduction partner.
    </FeatureItem>
  </FeatureList>
    </div>
    <AppCrossLinks
      variant="rail"
      apps={['larpingapp']}
      surface="product"
    />
  </div>
</Section>

<RotatingCards
  eyebrow="Three ways LarpingApp earns its place"
  title="Characters, scenes, players, all in one workspace."
  lede="Scroll the cards. Each pins as you reach it; the next slides up to cover."
  cards={[
    {
      tone: 'lavender',
      eyebrow: 'Characters',
      title: 'Sheets that survive a system switch.',
      summary: 'Every character is a typed record. Stats, background, skills, contacts, all fields with rules. Change the schema and every character migrates with it. Switching game system is a schema edit, not a rewrite.',
      cta: {label: 'See LarpingApp', href: '/apps/larpingapp'},
      panel: <AppMock app="larpingapp" />,
    },
    {
      tone: 'forest',
      eyebrow: 'Scenes',
      title: 'Linked NPCs, factions, locations.',
      summary: 'A scene has a location, participants, NPCs, and an outcome. All cross-linked, all queryable. Continuity becomes searchable; "who was at the moot in week six" is a click, not a memory test.',
      cta: {label: 'See OpenRegister', href: '/apps/openregister'},
      panel: <AppMock app="openregister" />,
    },
    {
      tone: 'workspace',
      eyebrow: 'Players',
      title: 'Each player gets their own dashboard.',
      summary: 'MyDash gives every player a personal view: their character stats, recent scenes, upcoming events. One Nextcloud login for the whole campaign, no separate player portal.',
      cta: {label: 'See MyDash', href: '/apps/mydash'},
      panel: <AppMock app="mydash" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="Widgets we ship"
  title="Your campaign on the Nextcloud home screen."
  lede="Install LarpingApp and these widgets show up on every player's dashboard. Character to the left, calendar to the right, scene log below."
  widgets={[
    {
      title: 'My character',
      desc: 'Character sheet snapshot. Stats, hit points, current scene, faction allegiance.',
      panel: (
        <div style={{display: 'flex', gap: 10, alignItems: 'flex-start'}}>
          <div style={{width: 44, height: 50, clipPath: 'var(--hex-pointy-top)', background: 'var(--c-lavender-300)', flexShrink: 0}}></div>
          <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 6}}>
            <div style={{height: 6, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
            <div style={{height: 4, width: '50%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
            {[{label: 'STR', val: '60%'}, {label: 'AGI', val: '80%'}, {label: 'WIS', val: '45%'}, {label: 'HP', val: '70%', accent: true}].map((row, i) => (
              <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
                <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 9, letterSpacing: '0.05em', color: 'var(--c-cobalt-400)', width: 22}}>{row.label}</div>
                <div style={{flex: 1, height: 4, background: 'var(--c-cobalt-100)', borderRadius: 1, position: 'relative'}}>
                  <div style={{height: '100%', width: row.val, background: row.accent ? 'var(--c-mint-500)' : 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                </div>
              </div>
            ))}
          </div>
        </div>
      ),
    },
    {
      title: 'Upcoming sessions',
      desc: 'Calendar events tagged for the campaign. Players see only their own faction sessions.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {date: '14', tone: 'var(--c-mint-500)'},
            {date: '21', tone: 'var(--c-lavender-300)'},
            {date: '28', tone: 'var(--c-forest-300)'},
            {date: '04', tone: 'var(--c-cobalt-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8}}>
              <div style={{width: 22, height: 25, clipPath: 'var(--hex-pointy-top)', background: row.tone, display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0}}>
                <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 9, fontWeight: 700, color: 'var(--c-cobalt-700)'}}>{row.date}</div>
              </div>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 3}}>
                <div style={{height: 4, width: '75%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '55%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Recent scenes',
      desc: 'Last sessions with location, NPCs, and outcome. One click back to the scene record.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {['The moot at Ravensford', 'Siege of Old Mill', 'Hunt for the wyrm', 'Council in Stonehollow'].map((_, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 3 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: i === 0 ? 'var(--c-orange-knvb)' : 'var(--c-mint-500)'}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 2}}>
                <div style={{height: 4, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '50%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{height: 3, width: 22, background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
            </div>
          ))}
        </div>
      ),
    },
  ]}
/>

<Showcase
  eyebrow="How LarpingApp plugs into the workspace"
  title="The campaign lives where the group already does."
  lede="No separate player portal. Three integration paths that keep the LARP inside the Nextcloud the group already uses."
  layout="side"
  defaultOpen="calendar-talk"
  items={[
    {
      id: 'calendar-talk',
      title: 'Calendar and Talk',
      summary: 'Sessions land in Calendar with the right faction filter; in-character chat lives in Talk channels per scene. The organiser invites once; players see it on the Nextcloud they already log in to.',
      cta: {label: 'See the integration guide', href: 'https://docs.conduction.nl/larpingapp/calendar-talk'},
      panel: <AppMock app="larpingapp" />,
    },
    {
      id: 'files',
      title: 'Files and Photos',
      summary: 'Character sheets, maps, and faction handbooks live in Files. Players upload session photos to a shared folder; the next scene pulls them in as references. Versioning is Nextcloud-native, no extra tooling.',
      cta: {label: 'See how to organise the campaign', href: 'https://docs.conduction.nl/larpingapp/files'},
      panel: <AppMock app="openregister" />,
    },
    {
      id: 'discord',
      title: 'Discord and Hilma sync',
      summary: 'Out-of-game communication stays on Discord; OpenConnector keeps the roster in sync so a new player joining the LARP also gets the right Discord role. One source of truth, two surfaces.',
      cta: {label: 'See the connector guide', href: 'https://docs.conduction.nl/larpingapp/discord-sync'},
      panel: <AppMock app="openconnector" />,
    },
  ]}
/>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Two apps that carry the campaign."
    align="stack"
  />
  <PairRow>
    <PairCard
      href="/apps/openregister"
      icon={<svg viewBox="0 0 24 24"><ellipse cx="12" cy="6" rx="8" ry="3"/><path d="M4 6v12c0 1.7 3.6 3 8 3s8-1.3 8-3V6"/></svg>}
      name="OpenRegister"
      why="Characters, NPCs, rules, and factions as typed registers. Schema edits migrate existing records along with them."
    />
    <PairCard
      href="/apps/mydash"
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/><rect x="14" y="12" width="7" height="9"/><rect x="3" y="16" width="7" height="5"/></svg>}
      name="MyDash"
      why="Per-player dashboard with your character stats, recent scenes, and upcoming events."
    />
  </PairRow>
</Section>

<CtaBanner
  title="A LARP without spreadsheets."
  lede={<>Install LarpingApp from the <span className="next-blue">Nextcloud</span> app store, import your existing characters into a schema, and run the next session without tab-shuffle.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'View on GitHub', href: 'https://github.com/ConductionNL/larpingapp'}}
/>
