---
title: Lokale demo, Conduction
description: Demo nodig? Draai Nextcloud lokaal met Docker, installeer de Conduction-apps uit de app store, zie ze werken. Geen partner nodig, geen agenda-uitnodiging, geen sales-call.
hide_table_of_contents: true
---

import {Section, SectionHead, HowSteps, HowStep, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, Button, ComposeBlock} from '@conduction/docusaurus-preset/components';

<Section spacing="default">
  <SectionHead
    eyebrow="Lokale demo"
    title={<>Demo nodig? Draai het zelf.</>}
    lede={<>De apps zijn open source, de install is gratis, de runtime past in een docker-compose-bestand. Drie commando's en je draait <span className="next-blue">Nextcloud</span> + Conduction op je laptop. Geen partnertraject, geen sales-call, geen agenda-uitnodiging. Demo voor je koffie afkoelt.</>}
  />

  <div style={{display: 'flex', gap: 14, marginTop: -16, flexWrap: 'wrap'}}>
    <Button variant="primary" size="lg" href="#docker-compose">Laat het compose-bestand zien</Button>
    <Button variant="secondary" size="lg" href="https://docs.nextcloud.com/server/latest/admin_manual/installation/docker.html">Lees de Nextcloud-docs</Button>
  </div>
</Section>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Hoe het werkt"
    title="Drie stappen, vijftien minuten."
    align="stack"
    lede="Nextcloud start binnen drie minuten. De Conduction-apps installeren zichzelf via de officiële app-store. Je hebt een werkende stack voor je tweede koffie."
  />
  <HowSteps>
    <HowStep title="Zet de docker-compose.yml in een lege map.">
      Het bestand hieronder volstaat: Nextcloud + Postgres + Redis. Sla op als <strong>docker-compose.yml</strong>, en draai <strong>docker compose up -d</strong>. Open <strong>http://localhost:8080</strong> en kies een admin-naam en wachtwoord.
    </HowStep>
    <HowStep title="Open de Nextcloud app store.">
      Log in als admin, klik je avatar rechtsboven, kies <strong>Apps</strong>. De Conduction-apps staan onder <strong>Integration</strong> en <strong>Office &amp; text</strong>. Eén klik per app installeert.
    </HowStep>
    <HowStep title="Configureer en speel.">
      OpenRegister levert een schema-bootstrap met voorbeelddata. OpenCatalogi indexeert de eerste registers. MyDash bouwt een dashboard van wat je hebt. Je kunt nu rondklikken zoals een bestaande klant.
    </HowStep>
  </HowSteps>
</Section>

<Section spacing="default" id="docker-compose">
  <SectionHead
    eyebrow="Het compose-bestand"
    title="Eén bestand, drie services."
    align="stack"
    lede="Postgres voor de data, Redis voor de cache, Nextcloud voor het werk. Allemaal officiële images. Geen custom build, geen Dockerfile. Past in een Slack-bericht."
  />

  ```yaml title="docker-compose.yml"
  services:
    db:
      image: postgres:16-alpine
      restart: unless-stopped
      volumes:
        - db:/var/lib/postgresql/data
      environment:
        POSTGRES_DB: nextcloud
        POSTGRES_USER: nextcloud
        POSTGRES_PASSWORD: nextcloud

    redis:
      image: redis:7-alpine
      restart: unless-stopped

    nextcloud:
      image: nextcloud:latest
      restart: unless-stopped
      depends_on: [db, redis]
      ports:
        - "8080:80"
      volumes:
        - nextcloud:/var/www/html
        - ./apps:/var/www/html/custom_apps   # optional: drop Conduction apps here for offline install
      environment:
        POSTGRES_HOST: db
        POSTGRES_DB: nextcloud
        POSTGRES_USER: nextcloud
        POSTGRES_PASSWORD: nextcloud
        REDIS_HOST: redis
        NEXTCLOUD_TRUSTED_DOMAINS: "localhost"

  volumes:
    db:
    nextcloud:
  ```

  <ComposeBlock filename="bash">{`# one-time setup
docker compose up -d

# tail the logs while Nextcloud bootstraps
docker compose logs -f nextcloud

# stop + clear (start fresh)
docker compose down -v`}</ComposeBlock>
</Section>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Kies de apps"
    title="Drie installs voor de eerste demo."
    align="stack"
    lede="Onze suggestie: begin met OpenRegister (de typed-data backbone), OpenCatalogi (publieke surface), en OpenConnector (haalt voorbeelddata binnen). Vijftien minuten samen, en je ziet de pipeline werken."
  />
  <PairRow>
    <PairCard
      href="/apps/openregister"
      icon={<svg viewBox="0 0 24 24"><ellipse cx="12" cy="6" rx="8" ry="3"/><path d="M4 6v12c0 1.7 3.6 3 8 3s8-1.3 8-3V6"/><path d="M4 12c0 1.7 3.6 3 8 3s8-1.3 8-3"/></svg>}
      name="OpenRegister"
      why="Installeer eerst. De typed-data foundation. Alle andere apps lezen ervan af."
    />
    <PairCard
      href="/apps/opencatalogi"
      icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4M3 17l9 4 9-4"/></svg>}
      name="OpenCatalogi"
      why="Maak elk register doorzoekbaar als publieke entry. Federation naar data.overheid.nl."
    />
    <PairCard
      href="/apps/openconnector"
      icon={<svg viewBox="0 0 24 24"><circle cx="6" cy="12" r="3"/><circle cx="18" cy="6" r="3"/><circle cx="18" cy="18" r="3"/><path d="M9 12h9M9 12l9-6M9 12l9 6"/></svg>}
      name="OpenConnector"
      why="Trekt voorbeelddata binnen via REST/SOAP/file drops. Alle 12 apps profiteren."
    />
  </PairRow>
</Section>

<Section spacing="default">
  <SectionHead
    eyebrow="Wat je krijgt"
    title="Een werkende stack die je kunt vertrouwen."
    align="stack"
    lede="Geen vooraf opgenomen screencast, geen demo-omgeving die offline kan. Je ziet precies wat een echte klant ziet, draaiend op jouw hardware. Bewaar het compose-bestand en laat het maandag aan een collega zien."
  />
  <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><path d="M12 7v5l3 3"/></svg>}
      title="Drie minuten van compose-up tot login."
    >
      <code>docker compose up -d</code> trekt de eerste keer 750MB binnen en start daarna binnen drie minuten. Volgende boots gaan in seconden.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M21 11.5a8.4 8.4 0 0 1-9 8.4 8.4 8.4 0 0 1-3.4-.7L3 21l1.5-5.4A8.4 8.4 0 1 1 21 11.5z"/></svg>}
      title="Officiële Nextcloud-image."
    >
      Geen custom Dockerfile. Wat je lokaal draait is identiek aan wat een klant in productie gebruikt. Versies, security-patches, alles via de release-cadence van Nextcloud.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/></svg>}
      title="Apps via de officiële app-store."
    >
      Geen lokale build. De Conduction-apps installeren zichzelf via de Nextcloud app-store, hetzelfde pad als productie. Geen "demo-only" magie.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>}
      title="Voorbeelddata via OpenRegister."
    >
      OpenRegister levert een schema-bootstrap met voorbeeldrecords. Je ziet typed data, audit log en stabiele citation-IDs werken vanaf de eerste minuut.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M12 2v4M12 18v4M2 12h4M18 12h4M5 5l3 3M16 16l3 3M5 19l3-3M16 8l3-3"/></svg>}
      title="Reset is één commando."
    >
      <code>docker compose down -v</code> verwijdert volumes en reset alles. Twee minuten later draai je opnieuw, schoon, klaar voor de volgende test.
    </FeatureItem>
  </FeatureList>
</Section>

<CtaBanner
  title="Demo nodig? Het draait op je laptop."
  lede="Drie commando's, één Nextcloud, alle Conduction-apps. Geen sales-call nodig. Stuur de docker-compose naar een collega als je het samen wilt zien."
  primaryCta={{label: 'Bekijk de apps', href: '/apps'}}
  secondaryCta={{label: 'Praat toch met een partner', href: '/partners'}}
/>
