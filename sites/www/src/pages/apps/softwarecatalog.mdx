---
title: SoftwareCatalog, Conduction
description: IT-asset management on Nextcloud. Software inventory, licenses, contracts, dependencies. One register, every install.
hide_table_of_contents: true
---

import {DetailHero, Section, SectionHead, FeatureList, FeatureItem, PairRow, PairCard, CtaBanner, AppMock, RotatingCards, WidgetShelf, Showcase, AgentTrace} from '@conduction/docusaurus-preset/components';

<DetailHero
  appId="softwarecatalog"
  crumb={[{label: 'Apps', href: '/apps'}, 'SoftwareCatalog']}
  status={{label: 'Stable', color: 'var(--c-mint-500)'}}
  version="v1.1"
  locales="NL · EN"
  title="SoftwareCatalog"
  tagline={<>IT-asset management on your <span className="next-blue">Nextcloud</span>. A central register of every application, licence, contract, and dependency in your organisation. Ideal for the IT department that wants to know what runs where.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Read the docs', href: 'https://docs.conduction.nl/softwarecatalog'}}
  tertiaryCta={{label: 'View on GitHub', href: 'https://github.com/ConductionNL/softwarecatalog'}}
  iconColor="var(--c-blue-cobalt)"
  icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/><rect x="14" y="12" width="7" height="9"/><rect x="3" y="16" width="7" height="5"/></svg>}
  illustration={<AppMock app="softwarecatalog" />}
/>

<Section spacing="default">
  <SectionHead
    eyebrow="What it does"
    title="One register, every install."
    align="stack"
    lede="SoftwareCatalog tracks which applications run where, on which server, under which licence, linked to which contract. It combines IT discovery with asset management. OpenCatalogi on top makes the whole catalogue searchable across the organisation."
  />
  <FeatureList>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/></svg>}
      title="Discovery via OpenConnector."
    >
      Pull your inventory from existing tools (Microsoft Intune, Jamf, GLPI, OCS Inventory) via OpenConnector. SoftwareCatalog deduplicates, normalises, and keeps it current.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><path d="M14 2v6h6"/></svg>}
      title="Licence and contract tracking."
    >
      Seats per application, contract period, cancellation date. Warnings before auto-renewal, not after.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><circle cx="6" cy="12" r="3"/><circle cx="18" cy="6" r="3"/><circle cx="18" cy="18" r="3"/><path d="M9 12h9M9 12l9-6M9 12l9 6"/></svg>}
      title="Dependency graph between applications."
    >
      App A runs on database B, on server C, with licence D. Click through to see the chain. Click "deprecation impact" to see who is affected if you remove something.
    </FeatureItem>
    <FeatureItem
      icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4M3 17l9 4 9-4"/></svg>}
      title="Federation to Forum Standaardisatie."
    >
      An OpenCatalogi layer on top publishes your software inventory (or a selection) to Forum Standaardisatie and data.overheid.nl. Other gemeenten see what you use and can federate from it.
    </FeatureItem>
  </FeatureList>
</Section>

<RotatingCards
  eyebrow="Three ways SoftwareCatalog earns its place"
  title="Discover, track, federate."
  lede="Scroll the cards. Each pins as you reach it; the next slides up to cover."
  cards={[
    {
      tone: 'lavender',
      eyebrow: 'Discover',
      title: 'Inventory pulled from your existing tools.',
      summary: 'OpenConnector pulls from Microsoft Intune, Jamf, GLPI, OCS Inventory. SoftwareCatalog deduplicates, normalises, and keeps the inventory current. No manual upkeep.',
      cta: {label: 'See OpenConnector', href: '/apps/openconnector'},
      panel: <AppMock app="openconnector" />,
    },
    {
      tone: 'forest',
      eyebrow: 'Track',
      title: 'Licences, contracts, dependency graph.',
      summary: 'Seats per application, contract period, cancellation date. Warnings before auto-renewal, not after. Click "deprecation impact" to see who is affected if you remove something.',
      cta: {label: 'See SoftwareCatalog', href: '/apps/softwarecatalog'},
      panel: <AppMock app="softwarecatalog" />,
    },
    {
      tone: 'workspace',
      eyebrow: 'Federate',
      title: 'Publish to Forum Standaardisatie.',
      summary: 'An OpenCatalogi layer on top publishes (a selection of) your inventory to Forum Standaardisatie and data.overheid.nl. Other gemeenten see what you use and federate from it.',
      cta: {label: 'See OpenCatalogi', href: '/apps/opencatalogi'},
      panel: <AppMock app="opencatalogi" />,
    },
  ]}
/>

<WidgetShelf
  eyebrow="Widgets we ship"
  title="What runs where, on the IT-lead's home screen."
  lede="Install SoftwareCatalog and these widgets show up on the dashboard for every IT-admin. Renewals first, inventory snapshot next, recent discovery deltas below."
  widgets={[
    {
      title: 'Renewals due',
      desc: 'Licences and contracts coming up. Pre-warning before the auto-renewal date, escalation after.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          <div style={{display: 'flex', alignItems: 'baseline', gap: 6, marginBottom: 4}}>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 28, fontWeight: 700, color: 'var(--c-orange-knvb)'}}>5</div>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 11, letterSpacing: '0.06em', textTransform: 'uppercase', color: 'var(--c-cobalt-400)'}}>this month</div>
          </div>
          {[{w: '70%', accent: true}, {w: '60%'}, {w: '55%'}, {w: '50%'}].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
              <span style={{width: 8, height: 9, clipPath: 'var(--hex-pointy-top)', background: row.accent ? 'var(--c-orange-knvb)' : 'var(--c-cobalt-300)'}}></span>
              <div style={{flex: 1, height: 4, background: 'var(--c-cobalt-200)', borderRadius: 1, width: row.w}}></div>
              <div style={{height: 4, width: 30, background: 'var(--c-cobalt-100)', borderRadius: 1}}></div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Inventory snapshot',
      desc: 'Apps tracked, total seats, deduplicated count. Categories below.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          <div style={{display: 'flex', alignItems: 'baseline', gap: 8, marginBottom: 4}}>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 22, fontWeight: 700, color: 'var(--c-cobalt-700)'}}>342</div>
            <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 9, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-400)'}}>apps tracked</div>
          </div>
          {[
            {label: 'productivity', val: '70%', tone: 'var(--c-cobalt-300)'},
            {label: 'security', val: '50%', tone: 'var(--c-mint-500)'},
            {label: 'devtools', val: '60%', tone: 'var(--c-lavender-300)'},
            {label: 'infra', val: '40%', tone: 'var(--c-forest-300)'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 6}}>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 8, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-500)', width: 60}}>{row.label}</div>
              <div style={{flex: 1, height: 5, background: 'var(--c-cobalt-100)', borderRadius: 1}}>
                <div style={{height: '100%', width: row.val, background: row.tone, borderRadius: 1}}></div>
              </div>
            </div>
          ))}
        </div>
      ),
    },
    {
      title: 'Discovery deltas',
      desc: 'New, updated, removed apps detected by the discovery sync. One click to the change record.',
      panel: (
        <div style={{display: 'flex', flexDirection: 'column', gap: 6}}>
          {[
            {tone: 'var(--c-mint-500)', stage: 'new'},
            {tone: 'var(--c-cobalt-300)', stage: 'updated'},
            {tone: 'var(--c-red-vermillion)', stage: 'removed'},
            {tone: 'var(--c-cobalt-300)', stage: 'updated'},
          ].map((row, i) => (
            <div key={i} style={{display: 'flex', alignItems: 'center', gap: 8, padding: '4px 0', borderBottom: i < 3 ? '1px solid var(--c-cobalt-50)' : 'none'}}>
              <span style={{width: 10, height: 11, clipPath: 'var(--hex-pointy-top)', background: row.tone}}></span>
              <div style={{flex: 1, display: 'flex', flexDirection: 'column', gap: 2}}>
                <div style={{height: 4, width: '70%', background: 'var(--c-cobalt-700)', borderRadius: 1}}></div>
                <div style={{height: 3, width: '50%', background: 'var(--c-cobalt-200)', borderRadius: 1}}></div>
              </div>
              <div style={{fontFamily: 'var(--conduction-typography-font-family-code)', fontSize: 8, letterSpacing: '0.05em', textTransform: 'uppercase', color: 'var(--c-cobalt-500)'}}>{row.stage}</div>
            </div>
          ))}
        </div>
      ),
    },
  ]}
/>

<Showcase
  eyebrow="How SoftwareCatalog plugs into the workspace"
  title="Discovery, alerts, and federation, mostly via apps you already run."
  lede="Three integration paths that keep the inventory accurate and surface the right signal in the right place. Most of the day-to-day plumbing rides on native Nextcloud apps."
  layout="side"
  defaultOpen="mail-notifications"
  items={[
    {
      id: 'mail-notifications',
      title: 'Mail, Notifications, Activity',
      summary: 'Renewal warnings land in Mail; the change feed shows up in Activity; high-severity alerts trigger a Notification on the IT-lead\'s screen. No separate alerting tool, no extra Slack hookup.',
      cta: {label: 'See the alerts guide', href: 'https://docs.conduction.nl/softwarecatalog/alerts'},
      panel: <AppMock app="softwarecatalog" />,
    },
    {
      id: 'discovery',
      title: 'Intune, Jamf, GLPI, OCS',
      summary: 'OpenConnector pulls inventory from your existing discovery tools nightly. SoftwareCatalog deduplicates, normalises, and writes the change feed back. New tools added without touching the schema.',
      cta: {label: 'See the connector guide', href: 'https://docs.conduction.nl/softwarecatalog/discovery'},
      panel: <AppMock app="openconnector" />,
    },
    {
      id: 'federation',
      title: 'Forum Standaardisatie federation',
      summary: 'An OpenCatalogi layer on top publishes (a selection of) the inventory to Forum Standaardisatie and data.overheid.nl. Other gemeenten see what you use; you read what they use.',
      cta: {label: 'See the federation guide', href: 'https://docs.conduction.nl/softwarecatalog/federation'},
      panel: <AppMock app="opencatalogi" />,
    },
  ]}
/>

<Section spacing="default" background="tinted">
  <SectionHead
    eyebrow="Pairs well with"
    title="Three apps that cover the IT-asset workflow."
    align="stack"
  />
  <PairRow>
    <PairCard
      href="/apps/openconnector"
      icon={<svg viewBox="0 0 24 24"><circle cx="6" cy="12" r="3"/><circle cx="18" cy="6" r="3"/><circle cx="18" cy="18" r="3"/><path d="M9 12h9M9 12l9-6M9 12l9 6"/></svg>}
      name="OpenConnector"
      why="Pulls inventory from your existing tools. Intune, Jamf, GLPI, OCS Inventory, all pre-built adapters."
    />
    <PairCard
      href="/apps/opencatalogi"
      icon={<svg viewBox="0 0 24 24"><path d="M3 7l9-4 9 4-9 4-9-4z"/><path d="M3 12l9 4 9-4M3 17l9 4 9-4"/></svg>}
      name="OpenCatalogi"
      why="Publishes (selected) inventory as a searchable public register. Federation to Forum Standaardisatie."
    />
    <PairCard
      href="/apps/mydash"
      icon={<svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="9"/><rect x="14" y="3" width="7" height="5"/><rect x="14" y="12" width="7" height="9"/></svg>}
      name="MyDash"
      why="Dashboards over the inventory. Which licences expire, which applications have the most seats."
    />
  </PairRow>
</Section>

<CtaBanner
  title="What runs where, under which licence."
  lede={<>SoftwareCatalog answers the IT questions nobody wants to ask on a Friday afternoon. Auto-discovery, contract tracking, dependency graph, all in one register.</>}
  primaryCta={{label: 'Install from app store', href: '/install'}}
  secondaryCta={{label: 'Talk to a partner', href: '/partners'}}
/>
